# Requirements for Windows - Versiones específicas con wheels pre-compilados
# Usar con: pip install --only-binary=:all: -r requirements-windows.txt

# Estas versiones tienen wheels disponibles para Python 3.9-3.12 en Windows
# Si usas Python 3.14, considera downgrade a Python 3.11

# Core scientific computing
numpy==1.26.4
scipy==1.11.4

# Visualization
matplotlib==3.8.2
seaborn==0.13.0

# Testing
pytest==7.4.3
pytest-cov==4.1.0

# NOTA: Si algún paquete falla, prueba con --only-binary=:all:
# pip install --only-binary=:all: -r requirements-windows.txt
