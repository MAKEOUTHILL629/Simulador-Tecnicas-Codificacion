# Simulador de Sistemas de Comunicación Multi-Generacional (5G/6G)
# Requisitos de Python - Core Dependencies

# IMPORTANTE: En Windows, instala con: pip install --only-binary=:all: -r requirements.txt
# Esto asegura que solo se usen wheels pre-compilados (no requiere compilador C)

# Core scientific computing
numpy>=1.21.0,<2.0.0
scipy>=1.7.0,<2.0.0

# Visualization
matplotlib>=3.4.0,<4.0.0
seaborn>=0.11.0,<1.0.0

# Testing
pytest>=7.0.0,<9.0.0
pytest-cov>=3.0.0,<6.0.0

# Note: For web interface (streamlit), install separately:
# pip install streamlit
# On Windows, if you encounter build errors, try:
# pip install --upgrade pip
# pip install --only-binary=:all: streamlit

# Optional: For advanced features
# See requirements-optional.txt for additional packages
